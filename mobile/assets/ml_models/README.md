Place licensed on-device model binaries here, for example:
- mobilenet_v3.tflite
- mobileclip_image.onnx or a TFLite equivalent
- face_embedding.tflite

Model binaries are not bundled because licenses and target-device optimizations vary.
